#include <stdio.h>
    int main(){
        printf("hello");
        int count = 0;
        void producer(void){
            int itemP;
            while (True)
            {
                producer_item(itemP);
                while (count ==N);
                Buffer[in] = itemP;
                in = (in+1)MODN;
                count = count +1;
            }
            
        }
    }

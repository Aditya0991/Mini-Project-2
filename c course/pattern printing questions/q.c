#include<stdio.h>
int main(){
    int n=5,row,col;
    for(row=0;row<=n;row++){
        for(col=0;col<n;col++){
            if((i+j)>(n-i)){
                printf("*")
            }
        }
    }
    }
    return 0;
}
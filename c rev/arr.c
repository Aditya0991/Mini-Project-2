#include<stdio.h>
int main(){
    int arr[] = arr[3];
    arr[1] = 0;
    printf("%d\n" , arr);
    // int size;
    // printf("Enter the size of array : ");
    // scanf("%d" , &size);
    // int arr[size];
    // for(int i = 0 ; i < size ; i++){
    //     scanf("%d" , &arr[i]);
    // }
    for(int i = 0 ; i < 5 ; i++){
        printf("arrays %d element is %d \n" , i,arr[i]);
    }
    
    return 0;
}